# 03 NEXT_STEPS —— 待办队列（含整改项与决策门）

> 更新：R22（Claude Code 接手轮）已完成 A1/A2/A3 三项整改、S2 纠错、B5 可判雏形。
> **R23 已重建新颖性判定（`oeis_index` + `novelty_gate`），并定位核心难题的机制。**

## A0. 核心难题的机制结论（R23，最重要）
**这台机器此前从未真正执行过新颖性检验**：裁判无法输出 N3、管线不调用裁判、记录不带整数序列、
引擎手写"惊喜候选"字符串与裁判双轨并行。重建后实测：
- **99 题中 72 题（73%）无机器可判数据，进不了新颖性门**；能进的 27 题 → N0×16 / N1×7 / N2×4 / N3×0。
- 六策略对照：经典组合计数 / 参数族停时表 / 模迭代轨道 / **math 引擎自身的表示计数模板** —— 幸存率**全为 0%**。
- **决定性发现：新颖性不是瓶颈，显著性才是。** 任选参数的序列同样"OEIS 未见 + 结构不可推"，
  即过 OEIS 门是**廉价的**；唯一拦得住的是**显著性证书**（具名对象的不变量 + 任意选择扰动下保持）。
- ⇒ **杠杆在生成侧**：要生成"自带不变量"的对象，而不是继续扩大"方向 × 载体"笛卡尔矩阵（已被 A 节证否）。

## A. 立即整改（优先级最高）—— **R22 已完成**
1. ~~**三域交叉去名义化**~~ ✅ **已完成**：`tools/tri_verifiers.py` 建 4 个真三域验证器，验收=参数敏感性；
   审计出 v3 的 50 条三域候选**真三域 0 条**，现真三域 4 条（旧 50 条全部剔除）。
2. ~~**F1 良构门槛升级**~~ ✅ **已完成**：`cross_md_v4.py::f1_check` 要求 obj/quant/crit 结构化三元；
   454 矩阵过门 0 条（实证自动短语全为空壳）。
3. ~~**矩阵质量分层**~~ ✅ **已完成**：(i)可判 8 / (ii)需建验证器 3 / (iii)空壳 454。

## B0. 生成侧重建（R23 结论指向的新优先级）—— **R24 已启动**
- ~~B0.1 补数据~~ **部分完成**: `combo` 已保留整数序列; 但 `math`/`records` 仍只存 fails_head。
- **B0.2 已做第一步**: 新增 `counterex_engine`(反例驱动) —— 首个**只提自己答不出的问题**的引擎。
- ~~B0.3 停止扩大笛卡尔矩阵~~ **已执行**: 不再加维度。
- **R24 新发现的缺口(下一轮)**: 13 个开放问题**过不了 novelty_gate**(门只判整数序列) -> 全计入"不可判"。
  需要建**问题级**的门(文献门/显著性), 而不是序列门。这是当前真正的瓶颈。

## B0'. 反例驱动(新主线, R24 起) —— R25 已端到端打通
- ✅ `counterex_engine`: 完整反例集 → 结构拟合 → 诚实延伸探针 → 开放问题(status=无法结算)。
- ✅ `tools/problem_gate.py`（问题级门）: D1 结构族 / **D2 机器前沿**(类库包含检查等) / D3 渐近信号。
  机器自行给出 6 条候选刻画(如 例外集 ⊆ {三角数} ∪ {216})。
- ✅ `tools/llm_judgment.py`（LLM/文献裁判台账, 带出处）。
- **两处机器独立重发现**: 质数+三角数 = **Zhi-Wei Sun 2008 猜想**(机器命中唯一例外 216);
  质数+平方数 = **Hardy-Littlewood 猜想 H**(机器例外集偶数列与文献吻合)。
- 裁决: N0×5 / N1×5 / N2×3 / **N3×0**。机器的问题**是真的但多数已被前人问过**。

## B0''. 下一步(当前最高优先级)
- **把反例驱动搬到"参照系稀薄的领地"**: 加性数论是数学中最拥挤的领域之一 —— 出路不是加对象类,
  而是换领地(迭代/递推/图不变量/非十进制表示/组合结构)。
- 3 条 N2(检索未见) **必须经专家或文献门**才能升级, **不许自称新**。
- 建**问题级文献门**的可自动化部分(如按族检索提示), 把"检索未见"与"确认未见"分开记录。

## B. 新颖性攻坚（继续）
4. ~~对 S2 停时表做正式 F3~~ ⚠️ **已纠错并降级**：旧"表观有界"结论**作废**（运行最大值同义反复 + 窗口过窄）。
   滑窗复算：p=3 **无界**（幂指数≈0.18），p=5/7 窗口内持平。S2 候选降级，不再作 M4 支撑。
   **剩余待办**：p=5/7 的"有界"目前只是"条件于到达 1 的窗口内持平"，需**全局有界性论证或反例**才算闭环。
5. ~~张力缺口深挖：言不尽意×熵率~~ ✅ **已做可判雏形**（`tools/p_A5_yanyi.py`）。改写成最小文法类分离问题，
   泵引理穷举机器判定 5/5 与预期一致。**诚实：组件为经典 N1，不主张新意。**
   **剩余待办**：把"意义模式"从形式语言扩展到**真实语料**驱动的模式（需专家/文献定标）。
6. 三域交叉中选 3–5 个少见组合（语言×生物×工程、美学×心理×信息）建验证器 —— **部分完成**（已有 4 个，
   但集中在"×信息"轴）。**剩余待办**：补"美学×心理×信息"等无信息轴组合。

## C. 发布收尾（P-D）
7. **可视化升级**：母题树（L0–L4）+ **树交叉视图** + 裁判徽章 + 张力线图谱。**用户 R22 明确重申此项重要。**
8. README/DESIGN 与现状同步；打 v1.0 tag。

## D. 新方向（用户 R22 追加）—— 经验/感知前端
9. **感知模块（一手经验 → 结构化自然语言）**：摄像头/麦克风 → 实体与事件抽取 → 结构化观测。
   之后接：结构化观测 → 日常疑问 → 真问题 → **AI4S 求解**，形成完整知识生产链。
   现状：**未开工**；需要视觉/语音模型接入（当前仓库纯标准库，无模型依赖）。

## E. 决策门（仅这些才停下问）
- G1 出现"机器真判 + OEIS/文献双未见 + LLM 不排除"候选 → 交人类/专家裁决；
- G2 阶段 DoD 完成 → 报结果，自动进下一阶段；
- G3 预算耗尽（累计 30 轮或用户喊停）→ 出边界结论；
- G4 用户方向输入 → 重新校准。

## F. 不要做的事（血泪教训，R22 新增第 5 条）
- 别用关键词灌当务分（必须走 `big_score_ev` 证据门）；
- 别把"名义交叉/名义三域"当成果（**R22 实测：v3 的 50 条三域候选真三域为 0**）；
- 别在参照系稠密空间（初等数论/课本题目）反复穷举；
- 别把"检索级未见"直接称作新问题（需文献/专家）；
- **别把"运行最大值单调"当成趋势发现**（S2 教训：需滑窗/分布统计量）；
- **别用"长度≥N"这类表面门槛冒充良构检查**（需对象+量词域+判据三元）。

## B0'''. **G1 决策门已触发(R32) —— 等人类裁决**
- 候选: **`Harshad数 + 平方数`** 的例外集(120 个, n∈[4,20000] 偶数)。
- 四门全过: 机器真判 ✅ / 机器无法描述(补 36 模类后仍) ✅ / OEIS 未见 ✅ / 显著性超零模型 ~10^5倍 ✅ / 检索无结果 ✅。
- **裁决单**: `docs/human_review_candidate.md`(含复现命令与 4 个待答问题)。
- 按 LONG_PLAN_V2 §5, **机器到此停止, 不自行宣称新问题**。
- 自纠错(第11次)记录: "机器无法描述"多半是类库太薄的产物; 补 36 模类后多数候选变可描述,
  `Harshad数+平方数` 是唯一仍无描述者 —— 故它也**可能**只是"类库还不够"。
