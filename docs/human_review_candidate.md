# ~~人类/专家裁决单~~ —— **已撤回**

> ## ⚠️ 本裁决单作废（R33，2026-09-10）
>
> 本单于 R32 发出，触发 G1 决策门。**R33 复核发现两处机器侧错误，结论全部推翻，G1 为误触发。**
>
> | 原主张 | 复核结果 |
> |---|---|
> | "超出零模型 **~1.2×10⁵ 倍**" | ❌ **错 5 个数量级**。首版 Poisson 零模型假设独立（λ=16.12→预测 0.001），但 Harshad 集高度结构化、r(n) 远**超离散**。正确的**置换零模型**（保持尺寸+奇偶，重抽）给出：实测 **120** vs 零均值 **39.5** → **3.04×**，**p ≈ 0.016** |
> | "机器**无法描述**例外集（补 36 个模类后仍无描述）" | ❌ **假象**。类库缺"**避开型**"原语与**二次剩余类**。补上后该例外集**可以描述**：**避开 QR(mod 18)**（模 18 的二次剩余，覆盖扫描空间 44.5%）。候选分数从 3 降到 **0** |
>
> **结论**：`Harshad数+平方数` **既不够显著（3×, p≈0.02），也不是不可描述**。
> 两项关键证据均为**机器侧缺陷**所致，不是发现。**G1 不应触发，机器不应升级到人类。**
>
> 附：补足原语后，**39 个候选中无一"无法描述"** —— 说明"机器无法描述"这一门
> 在此前的实现下系统性不可靠，凡据其得出的结论都应重审。

---

## 原内容（留档，勿使用）

**原候选**：偶数 n 写成 (Harshad 数) + (平方数) 的例外集。
**原四门**：机器真判 ✅ / 机器无法描述 ✅ / OEIS 未见 ✅ / 显著性 10⁵× ✅ / 检索无结果 ✅。

**原请专家回答**：① 已知刻画？② 文献出处？③ 10⁵ 倍超额的解释？④ 值得研究吗？

---

## 这一轮真正的教训（保留）

1. **显著性判据必须用置换零模型，不能用 Poisson。** 结构性集合（Harshad）天然超离散，
   Poisson 会把平凡效应放大成天文数字。**判据本身错了，数字再大也没有意义。**
2. **"机器无法描述"必须连同"类库完备性"一起报告。** 机器说"描述不了"，可能只是
   它的工具太少 —— 本例中补两个原语（避开型、二次剩余）后，全部 39 个候选都变得可描述。
3. **同一个 `break`-on-unsorted 的坑，本轮又踩了两次**（临时脚本 + 零模型循环内部），
   每次都伪造出大量假例外。已改为**函数内部强制排序**，不再依赖调用方。
4. **G1 的价值在于它逼出了上面三条。** 误触发本身是坏结果，但复核过程产生了真实的方法学修正。
